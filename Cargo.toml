[workspace]
members = [
    "dap-client",
    "fuel-core",
    "tx-client",
    "indexer-lib",
    "index-runner",
    "examples/simple-wasm"
]

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
