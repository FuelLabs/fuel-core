[package]
authors = ["Fuel Labs <contact@fuel.sh>"]
categories = ["cryptography::cryptocurrencies"]
description = "A Tokio tracing initializer for testing"
edition = "2021"
homepage = "https://fuel.network/"
keywords = [
    "blockchain",
    "cryptocurrencies",
    "fuel-client",
    "fuel-core",
    "fuel-tracing",
    "tracing",
]
license = "BUSL-1.1"
name = "fuel-core-trace"
publish = false
repository = "https://github.com/FuelLabs/fuel-core"
version = "0.0.1"

[dependencies]
ctor = "0.1"
tracing-appender = "0.2"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[dev-dependencies]
tracing = { version = "0.1" }
