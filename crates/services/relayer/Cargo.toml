[package]
name = "fuel-core-relayer"
version = "0.15.1"
authors = ["Fuel Labs <contact@fuel.sh>"]
edition = "2021"
homepage = "https://fuel.network/"
keywords = ["blockchain", "fuel", "fuel-vm"]
license = "BUSL-1.1"
repository = "https://github.com/FuelLabs/fuel-core"
description = "Fuel Relayer"

[dependencies]
anyhow = "1.0"
async-trait = "0.1"
bytes = "1.1"
ethers-contract = { version = "1.0.2", default-features = false, features = [
    "abigen",
] }
ethers-core = { version = "1.0.2", default-features = false }
ethers-providers = { version = "1.0.2", default-features = false, features = [
    "ws",
    "rustls",
] }
fuel-core-interfaces = { path = "../../fuel-core-interfaces", package = "fuel-core-interfaces", version = "0.15.1" }
fuel-core-storage = { path = "../../storage", version = "0.15.1" }
fuel-core-types = { path = "../../types", version = "0.15.1" }
futures = "0.3"
once_cell = "1.4"
parking_lot = "0.12"
serde = "1.0"
serde_json = "1.0"
thiserror = "1.0"
tokio = { version = "1.21", features = ["macros"] }
tracing = "0.1"
url = "2.2"

[dev-dependencies]
fuel-core-interfaces = { path = "../../fuel-core-interfaces", package = "fuel-core-interfaces" }
mockall = "0.11"
test-case = "2.2"
tokio = { version = "1.21", features = ["macros", "test-util"] }

[features]
test-helpers = ["fuel-core-types/test-helpers"]
