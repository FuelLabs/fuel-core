[package]
authors = ["Fuel Labs <contact@fuel.sh>"]
categories = ["cryptography::cryptocurrencies"]
description = "Storage types and primitives used by Fuel core protocol."
edition = "2021"
homepage = "https://fuel.network/"
keywords = [
    "blockchain",
    "cryptocurrencies",
    "fuel-client",
    "fuel-core",
    "fuel-storage",
]
license = "BUSL-1.1"
name = "fuel-core-storage"
repository = "https://github.com/FuelLabs/fuel-core"
version = "0.15.1"

[dependencies]
anyhow = "1.0"
fuel-core-types = { path = "../types", version = "0.15.1" }
fuel-vm-private = { version = "0.24", package = "fuel-vm" }
thiserror = "1.0"

[features]
test-helpers = []
