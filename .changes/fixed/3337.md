Start network sync only after importer consumers have completed startup recovery, preventing importer notification backpressure from repeatedly timing out during off-chain catch-up. Block production remains gated on node readiness.

Update the locked `rustls` dependency to 0.23.45 to address RUSTSEC-2026-0285.
