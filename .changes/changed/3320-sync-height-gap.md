PoA Ready is a live reserved-peer Height Gap check (`network height − local DB height ≤ --max-sync-height-diff`, default 1), snapshotted from P2P rather than a duplicated SyncTask peer map. Isolated/dev (`--min-connected-reserved-peers=0`) skips the comparison. `--time-until-synced` remains ignored (CLI compat). `/v1/health` is unchanged.
