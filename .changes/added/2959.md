Added new field to compressed block header - `prev_compressed_block_root` which is the root of the `CompressedBlock` table, at the previous height.
  - Includes validation of this field in decompression.
