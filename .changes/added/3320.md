Add `/v1/ready` HTTP endpoint reporting whether the node has finished startup and is p2p-synced (`200`/`503` plus a JSON breakdown of each condition), for use as a Kubernetes readiness/startup probe target distinct from the process-only `/v1/health` liveness check.
