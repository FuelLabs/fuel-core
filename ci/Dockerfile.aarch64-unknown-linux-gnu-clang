FROM ubuntu:24.04

ENV PKG_CONFIG_ALLOW_CROSS="true"

RUN dpkg --add-architecture arm64 && \
    apt-get update && \
    apt-get install --assume-yes make clang libclang-dev binutils-aarch64-linux-gnu zlib1g-dev:arm64
