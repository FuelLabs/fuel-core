scalar ID
scalar Address
scalar Bytes32
scalar Color
scalar ContractId
scalar Salt

enum IndexType {
    Btree,
    Hash,
    Brin,
}

directive @index(type: IndexType) on FIELD_DEFINITION | ENUM_VALUE

directive @foreign_key(
    key: String
) on FIELD_DEFINITION | ENUM_VALUE
